# chats-api
Тестовое задание для HiTalent: REST API для чатов и сообщений на Go.
##  Описание
Проект реализует простой API для работы с чатами и сообщениями:
- **CRUD для чатов**
- **Отправка сообщений в чаты**
- PostgreSQL в качестве БД (через GORM)
- Миграции через Goose
- Docker + Docker Compose для удобного запуска

Проект полностью готов к запуску в контейнерах.

---

##  Быстрый старт

1. **Клонировать репозиторий**

```bash
git clone https://github.com/1Str1de1/chats-api.git
cd chats-api
```

2. **Скопировать пример окружения**
```bash
cp .env.sample .env
```
3. **Запустить через Docker Compose**
```bash
docker compose up --build
```

Будет:
- запущена PostgreSQL
- применены миграции
- запущен API-сервер

##  Возможности

✔ Создание и получение чатов  
✔ Создание и получение сообщений в чатах  
✔ Валидация входных данных  
✔ Чистая архитектура (handler → service → repository)  
✔ Юнит-тесты с моками (`testify`)  
✔ Автоматический запуск миграций при старте

---

##  Стек

| Компонент | Технология |
|----------|-------------|
| Язык | Go |
| Роутинг | `net/http` |
| ORM | GORM |
| Миграции | Goose |
| База данных | PostgreSQL |
| Тесты | `testify`, `httptest` |
| Деплой | Docker, Docker Compose |

---

🧠 API Эндпоинты
```bash
POST   /api/chats                → создать чат
GET    /api/chats                → список чатов

POST   /api/chats/{id}/messages  → отправить сообщение в чат
GET    /api/chats/{id}/messages  → получить сообщения чата
```

Пример запроса "создать чат":
```bash
curl -X POST http://localhost:8080/api/v1/chats \
  -H "Content-Type: application/json" \
  -d '{"title":"Новый чат"}'
```